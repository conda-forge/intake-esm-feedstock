{% set name = "intake-esm" %}
{% set version = "2019.4.26.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 1975f7ab4a565076267d77056ad7af9b39bf6f27a0993d1ed084fed2e52f76c3

build: 
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"
  number: 0
  noarch: python

requirements:

  host:
    - python >3.5
    - pip
    - setuptools_scm
  


  run:
    - python >3.5
    - intake-xarray
    - tqdm

test:
  imports:
    - intake_esm

about:
  
  home: https://github.com/NCAR/intake-esm
  license: Apache
  license_file: LICENSE
  summary: |
    Intake-esm provides a plugin for building and loading intake catalogs for earth system data sets holdings, such as CMIP and CESM Large Ensemble datasets.  
  dev_url: https://github.com/NCAR/intake-esm
  doc_url: https://intake-esm.readthedocs.io/en/latest/

extra:
  recipe-maintainers:
    - andersy005
    - matt-long
    - jukent
    - kmpaul
